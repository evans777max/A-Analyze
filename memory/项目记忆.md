# A-Analyze · 项目记忆（公开版）

> 跨会话记忆锚点 · 公开脱敏版：只含项目状态、决策与约定；本地环境细节维护在仓库外的私有记忆中（双层记忆约定见 §3）。
> 更新：2026-08-10 收盘后

## §1 当前状态

- **版本**：App v4.1.2 · Model v4.0 · History Schema v2（机器可读：`data/model-meta.json`）
- **主入口**：GitHub Pages（EdgeOne 默认域名对内地受限，待自定义域名恢复）
- **数据管线**：双 cron 收盘快照（北京 15:20 / 17:20 兜底，workflow 级并发保护 + 提交前校验）；2026-08-10 起为 v4.0 实录纪元（记录带 mv/sv + 分层字段），首日双 cron 幂等覆盖已实测（同日 26 条无重复）
- **治理**：`scripts/validate-repo.mjs` 18 项检查（口径锚点 / 数据质量 / 版本一致性 / 隐私扫描）在 push/PR 与快照提交前强制执行
- **审计结论定调**（详见 `docs/MODEL_CARD.md` 与 `memory/A股规律观察.md`）：评分 = 状态强度而非收益预测；模型价值在风险警示（过热 / 状态透支 / 退潮高分），全部为样本内证据，2026-08-10 起积累样本外实录

## §2 常设约定

1. **收盘日更**：每交易日收盘后核对快照落地、结算观察日志中的待验证项（O 系列）、更新记忆
2. **周度复盘**：每周五收盘后出当周复盘，MD（GitHub 直读）+ HTML（Pages）双格式，流金简版另出一页纸 PDF；README「周度复盘记录」表登记
3. **模型改动纪律**：页面 / snapshot / audit 三处同口径，validate-repo 锚点校验守护；模型口径变更需升 Model Version 并在 model-meta 纪元表登记
4. **观察→固化流程**：盘面现象先入观察日志（O 系列，含可证伪的验证条件）→ 收盘 / 多样本验证 → 固化为规则（R 系列）并进模型；反方向同样成立（审计判负即退役，如 rsPen）
5. **推演纪律**：已验证的审计规律优先级高于叙事推理（2026-08-10 教训固化）；盘中结论一律以收盘为准（R3，双向适用）

## §3 记忆分层（2026-08-10 确立）

| 层 | 位置 | 内容 |
|---|---|---|
| 公开层 | 仓库 `memory/`（本目录） | 项目状态、决策、约定、市场观察日志、会话日志 |
| 私有层 | 本地（不入库） | 本机环境、路径、工具链踩坑、个人工作流细节 |

公开层随仓库同步、受 CI 隐私扫描门禁；`.kiro/` 保持永久不追踪（Agent 工作目录）。

**公开层文件分工**：`项目记忆.md`（本文件，项目状态锚点）· `A股规律观察.md`（市场规律 R/O 系列）· `会话日志.md`（人机协作逐轮记录，2026-08-12 建立并补录 08-11 起；只记结论与决策，不逐字抄录对话；当日多轮累积，收盘后合并提交一次）· `筛选存档_2026-08.txt`（标的池筛选留档）。

## §4 决策时间线（摘要）

- 08-04 仓库建立，v2.0 四层评分模型上线，EdgeOne 部署
- 08-05 汉鑫 97 分事件 → R2 过热惩罚（v3.2）
- 08-06 环境水位提示、分时线、RS 趋势因子（v3.3-3.4）
- 08-07 无利空闪崩 V 回 → R3 假摔警示（v3.9）；快照管线 + 60 日回填（v3.6-3.8）；首期周报；EdgeOne 默认域名内地受限 → 周报 MD 版
- 08-08 模型体检（60日 + 全年 IC 审计）+ 跨市场传导审计 → **v4.0 证据驱动校准**（rsPen 退役 / L2 时变权重 / 状态透支与退潮高分警示 / 快照扩字段）
- 08-10 repo 转公开 + GitHub Pages 主入口；v4.1 公开仓库加固（隐私 / 版本治理 / CI）；v4.1.1 北京时间修复；v4.1.2 L2 标签同步；首条版本化快照落地；记忆分层并回归仓库同步
- 市场侧：O1「中报脉冲抢跑→T+1 拉高兑现→T+2 补跌」三日周期闭环（候选固化）；O5 流金 7.35 六测六收回；O7 定性为高位科技系统性减仓（锚传导 risk-off 期失效）

## §5 待办 / 待验证

- [ ] 样本外月度复检（2026-09 起，用 v4.0 实录快照重跑 audit）
- [ ] 板块轮动因子与尾盘分化雷达（O7 产品化，见 `docs/ROADMAP.md`）
- [ ] 自定义域名（恢复 EdgeOne 入口）
- [ ] License 决策（当前未授予开源许可证）
- [ ] Git 历史中旧内部记忆的处理决策（保留现状 / 历史重写需单独批准）
- [ ] O1 三日周期在下一个财报季标的上复验；流金中报预约披露日核查

## §6 环境与工具链备忘（泛化脱敏版，2026-08-10 自私有层迁移）

**行情数据源**
- 腾讯 `qt.gtimg.cn` 返回 GBK 编码，Node 侧用 `TextDecoder('gbk')` 解码；字段 30=时间戳、37=成交额(万)、38=换手、7/8=外盘/内盘
- **腾讯 `f[6]` 成交量单位按板块分裂（2026-08-18 实测 19 只无例外）**：科创板 `sh688xxx` 返回**股**（6/6，与新浪同单位）；沪主板 `sh60x`、深主板 `sz00x`、创业板 `sz30x`、北交所 `bj92x` 返回**手**（13/13）。**订正**：此前"腾讯/东财一律为手、与新浪恰好相差 100 倍"的表述不成立。合成当日 bar 时若一律 ×100 折算到股，科创板量能会被放大 100 倍 → 量比失真（08-14、08-18 两次复现，偏差恒落在 688 段，见 `A股规律观察.md` O10 陷阱A）
- 新浪日K `datalen` 可取 260+ 根（覆盖整年）；**当日 bar 发布晚于收盘**（08-14 实测 15:02 无 / 17:12 有；08-18 约 15:58 才有），需用 qt 实时收盘价兜底补当日。**兜底必须先比对最后一根的 `day`**：`index.html` 的 `liveBars()` 与 `scripts/snapshot.mjs` 均缺此校验，会把今价写进昨日 bar → MA 窗口/20日区间/量比同时错位一日（见 O10 陷阱B；快照目前靠 cron 晚点侥幸未中招）
- 东财接口在部分网络环境不可达，页面已按可用因子自动降级；美/日/韩指数日K可用 Yahoo chart API（`query1.finance.yahoo.com/v8/finance/chart/`）
- 北交所已整体迁至 920 代码段，旧 83/87/43 段代码返回停牌快照
- 北交所收盘价由 14:57-15:00 集合竞价决定，竞价盘薄，数百手即可使收盘价偏离连续竞价尾价 1%+
- **东财板块接口 `push2.eastmoney.com/api/qt/clist/get` 会 302 跳转到 `push2delay`（延时源）**——用 `-L` 跟随虽能取到数据但**是延时数据**，做盘中归因会得出错误结论（2026-08-18 实测踩坑）；该端点亦频繁 302/502。板块级实时数据无稳定免费源，替代 = 腾讯 `qt.gtimg.cn` 批量取个股后自行按方向归类
- 东财 `push2his`（日K）在本机 Node fetch 被拒（UND_ERR_SOCKET）而 `push2`（日韩指数）可用；后者仍间歇 302/502，snapshot.mjs 已加 3 次重试 + 失败 WARN

**快讯数据源（2026-08-18 新增，用于异动归因；工具 = `scripts/newsfeed.mjs`）**
- ✅ 同花顺 `news.10jqka.com.cn/tapp/news/push/stock/` —— **秒级 `ctime` 时间戳**，做时点对齐首选；Node fetch 直连可用
- ✅ 华尔街见闻 `api-one-wscn.awtmt.com/apiv1/content/lives` —— 可用；部分条目 `title` 为空（正文在 `content_text`），但**名单类快讯更全**，与同花顺互补（08-18 实例：同花顺只报"诚迈涨超9%"，见闻给出完整信创名单）
- ❌ 财联社 `cls.cn/nodeapi/telegraphList` —— HTTP 404
- ❌ 东财快讯 `np-listapi.eastmoney.com` —— 返回 87 字节空壳
- 时区口径：两源时间戳均为 Unix 秒，脚本内统一按 UTC+8 换算，不依赖本机 TZ

**GitHub / CI**
- Actions 定时任务常延迟 2-3 小时（已用双 cron + 手动 dispatch 兜底，workflow 级并发保护）。实测落库窗口 **15:57–18:20**（08-18 最早 15:57，08-14 最晚 18:18）
- **手动 dispatch 的前置条件**：必须先确认新浪已发布当日日K 再触发，否则 `snapshot.mjs` 会按错位一日的 bar 落库脏数据（无日期校验，见 MODEL_CARD #9）
- **人机协作记录的同步保障**（2026-08-18 决策）：`data/history.json` 由 Actions 服务器端保证，与会话无关；但 `memory/` 下三份记录只能由对话生成，依赖本地自动任务（工作日 16:47，已设为 **durable** 持久化，Claude 重启后自动恢复）。残留失效点：recurring 任务 7 天自动过期需定期重建；且仅在 REPL 空闲时触发，盘中问答时段会被挤掉。**仓库层面未强制**（曾提议 validate-repo 校验"每个交易日必须有对应日志标题"，未采纳），故漏记只能靠 cron + 对话提醒发现
- `GITHUB_TOKEN` 的 bot 提交不会触发其他 workflow（防递归机制）——因此快照数据的校验放在管线内"先校验后提交"步骤，而非依赖 push 触发
- GitHub 网页不渲染 HTML 文件（只显源码）；其内置 PDF 预览对 chromium 生成的中文子集字体 PDF 可能报错（文件本身无损，下载后正常）——网页分发走 Pages
- GitHub Pages 新子域名首次启用有 CDN 传播窗口，期间部分节点返回 404 且可能被缓存约 10 分钟，加查询参数可绕过

**验证方法论**
- 线上可达性结论必须区分"服务端在线"与"目标地区用户可达"：本机网络出口地区可能与目标用户不同，仅凭本机测试（尤其 IP 直连绕 DNS）会产生系统性误判——曾因此误判过页面托管平台的内地可用性
- 盘中记录的数字必须在收盘后用快照数据复核（盘感与盘中读数会出错）
- Playwright：`file://` 下页面 fetch 本地 JSON 会失败（页面需优雅降级）；等待外部资源用 `load` 而非 `networkidle`（外链可能挂起）

**Windows/PowerShell（本项目脚本编写注意）**
- PowerShell 5.1 将无 BOM 的 UTF-8 `.ps1` 按 ANSI 解析：脚本含中文/全角字符会乱码甚至解析失败——临时脚本优先用 Node/Python，或保持纯 ASCII
- PowerShell 管道/重定向会破坏二进制与 UTF-8 流：下载二进制用 `curl.exe -o` 或由 Node/Python 直接抓取，勿经由 shell 管道
- 新写入的脚本文件立即执行可能遇到可见性竞态：执行报"文件不存在"时等待数秒重试即可
