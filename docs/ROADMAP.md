# A-Analyze · Roadmap（公开待办）

> 只列可公开的产品/工程方向；完成即移入 README 版本记录。

## 模型（等待样本外证据后推进）

- [ ] **样本外复检**：2026-09 起用 v4.0 实录快照（含分层分字段）月度重跑 `audit.mjs`，检验样本内结论（过热惩罚、状态透支、退潮反转）是否在样本外成立
- [ ] **板块轮动因子**：按池内板块 tag 计算篮子相对强弱与资金迁移方向，作为 L3 的补充维度（依据：联动类信号呈一日效应且行情依赖）
- [ ] **尾盘分化雷达**：实时按板块分组统计"午后高点回撤 vs 尾盘强度"，把人工分时扫描产品化
- [ ] **L2 锚差异化**：跨市场审计显示不同美股锚对不同板块传导强度差异大且随时间漂移，映射提示可按锚-板块对分别加权
- [ ] **预测留痕与命中率**：快照记录当日触发的警示状态，积累后输出各警示的命中率统计
- [ ] **量能因子平盘不连续**（2026-08-11 发现）：量比得分在 `pct = 0` 处硬跳变，缩量时最严重——实测 vr=0.70 时层内跳 89.5 分、总分 6.2 分并跨一档，收平的缩量票被顶格判负，且会出现"收跌评分反而更高"的读数。候选修正 = 平盘窄区间（如 |pct| < 0.3%）内两分支线性过渡。**改动会改变模型行为、打断 v4.0 实录可比性**，须按新纪元处理，故与首轮样本外复检（2026-09）一并决策；当前已作为已知偏差记入 `docs/MODEL_CARD.md`

## 工程

- [ ] **model-core 提取**：将评分模型抽为单一实现（页面与快照共用），彻底消除双实现漂移风险；约束 = 保持单文件离线可用（构建期内联或运行时可选加载），需通过逐日回归比对后才切换
      · 2026-08-11 实例：`snapshot.mjs` 的 L2 缺日经/KOSPI 两项（页面有），权重锚点全绿仍产生层内 14.1 分漂移——已修并补 L2 行项锚点校验，但双实现风险的根因仍在
- [ ] **周报数据包脚本**：`scripts/weekly.mjs` 固化周度数据采集（指数周表、池周表、尾盘扫描），消除手工数字口径错误
- [ ] **CI 供应链加固**：Actions 依赖（checkout/setup-node）按 SHA 锁定的评估
- [ ] **YAML 静态校验**：为 workflows 引入正式 YAML/actionlint 校验（当前仅基础检查）

## 分发

- [ ] **自定义域名**：绑定后恢复 EdgeOne 入口（或直接绑到 GitHub Pages），解决默认域名地域限制
- [ ] 周报自动索引页（weekly/ 目录下生成 index）

## 治理

- [ ] License 决策（当前未授予开源许可证，见 README 说明）
- [ ] Git 历史中旧内部记忆的处理决策（当前保留历史、不重写；如需彻底清除需单独批准的历史重写方案）
